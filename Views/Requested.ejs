</html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title> Admin </title>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
    <style media="screen">
        * {
            font-family: Montserrat, "Helvetica Neue", Helvetica, Arial, sans-serif;
            font-weight: bolder;
        }

        .coloumn {
            margin: 0;
        }

        .navbar-default {
            height: 100px;
            padding-top: 23px;
            padding-left: 70px;
            border: 0;
            border-radius: 0 !important;
            background: #2c3e50;
        }

        .navbar-default .navbar-brand {
            text-transform: uppercase;
            font-size: 2em;
            text-decoration: none;
            color: white;
            transition: color 0.3s ease;
            cursor: pointer;
        }

        .navbar-default .navbar-brand:hover {
            color: #18bc9c;
            transition: color 0.3s ease;
        }

        .card {
            padding: 10px;
            padding-top: 20px;
            background: #18bc9c;
            border: 1px #c8c3ff solid;
            border-radius: 5px;
            text-align: center;
            margin: 10px 3% 1% 3%;
            height: 190px;
            width: calc(80% * (1/3));
        }

        .card .card-BNAME {
            color: #02695e;
            font-size: 10em;
            font-weight: bold;
            text-transform: capitalize;
        }

        .card .card-verify:hover {
            color: #18bc9c;
            background: BLACK;
            transition: background-color 0.2s ease;
        }

        .form-container {
            height: 400px;
            border-left: 1px solid gray;
            padding-left: 30px;
        }

        .form-control {
            width: 90%;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/"> The Request to join the platform </a>
            </div>
        </div>
    </nav>
    <div class="row">
        <div class="col-xs-12 col-md-8">
            <div class="row">
                <% users.forEach(function(user){
                  %>

              <div class="card col-xs-4">
              <h4 class="card-BNAME"> <%= user.Buisness_name %></h4>
                  <a href="/verify/<%= user.id%>" class="btn btn-primary card-verify">
                  Verifyyy this Buissness <form method="POST" action="/verify/<%= user.id%>"> </a>
                      </div>

                  <%
                })
               %>


            </div>
        </div>

        </div>
    </div>

</html>
